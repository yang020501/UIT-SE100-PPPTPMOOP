<Window x:Class="household_management.View.Search"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:household_management.View"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Search" Height="450" Width="800" WindowStartupLocation="CenterScreen" AllowsTransparency="True" WindowStyle="None">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.07*"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="#FF4D60C0" HorizontalAlignment="Right" >
            <Image  Source="/household_management;component/Resources/search.jpg" Stretch="Fill" Margin="12,3,760,3.6"/>


            <Button x:Name="btnExit"   Cursor="Hand" Click="btnExit_Click" Margin="770,2,3,1.6" Height="Auto" RenderTransformOrigin="0.5,0.515">
                <Button.Background>
                    <ImageBrush ImageSource="/household_management;component/Resources/exite.jpg"/>
                </Button.Background>
            </Button>
            <Label Content="Search"  FontWeight="Bold" FontSize="14" Foreground="#DDE1E6F3" Margin="40,1,657,1.6"/>

        </Grid>
        <Grid Grid.Row="1">
            <TabControl
                Style="{StaticResource MaterialDesignNavigatilRailTabControl}"
                materialDesign:ColorZoneAssist.Mode="SecondaryMid">
                <TabItem
                    Header="{materialDesign:PackIcon Kind=Heart, Size=24}"
                    Style="{StaticResource MaterialDesignNavigationRailTabItem}">
                    <TextBlock>
                    <Run
                         Text="tab 1 content. Default look and behaviors." />
                     <LineBreak />
                     <LineBreak />
                      <Run
                         FontStyle="Italic">
     
                       </Run>
                    </TextBlock>
                </TabItem>
                <TabItem
                     Header="{materialDesign:PackIcon Kind=Spade, Size=24}"
                     Style="{StaticResource MaterialDesignNavigationRailTabItem}">
                    <ListView>
                        
                    </ListView>
                </TabItem>
            </TabControl>
        </Grid>

    </Grid>
</Window>
